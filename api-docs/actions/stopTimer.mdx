---
title: ".stopTimer()"
---

## Usage Example

```java
// VCTAPI.stopTimer(sender)
VCTAPI.stopTimer(null);
```

## Parameters

<ResponseField name="sender" type="CommandSender">
  The one who sent the command; use `null` if there is no player.
</ResponseField>